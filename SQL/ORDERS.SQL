CREATE TABLE ORDERS(
ID NUMBER,
CONSTRAINT ORDER_ID PRIMARY KEY (ID),
CONSTRAINT ID_CHECK CHECK (ID<1000),
USER_ID NUMBER UNIQUE,
ROOM_ID NUMBER UNIQUE,
DATE_FROM TIMESTAMP,
DATE_TO TIMESTAMP,
MONEY_PAID NUMBER(9,2)
);